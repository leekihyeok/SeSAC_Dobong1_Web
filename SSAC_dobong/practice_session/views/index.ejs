<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('head', {title: 'HOME'}) %>
  </head>
  <body>
    <header><%- include('header') %></header>
    <h1>session 실습</h1>
    <p>여기는 홈입니다.</p>

    <!-- 로그인 되었다면 -->
    <% if(isLogin) {%>
    <p><%=user.userId%>님 환영합니다.</p>
    <% } else {%>
    <!-- 로그인이 되지 않았다면 -->
    <p>로그인을 아직 하지 않았습니다.</p>
    <% } %>
  </body>
</html>
